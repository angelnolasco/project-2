<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/public/show.css">
    <title>project-2</title>
</head>
<body>
    <h1><%=fighter.name%> profile </h1>
    <ul>
        <% if (fighter.type) { %>
            <% for (let i = 0; i < fighter.type.length; i++) { %>
              <li><%= fighter.type[i] %></li>
            <% } %>
          <% } %>
    </ul>
    <p>Name:<%= fighter.name%></p>
    <p>Age:<%= fighter.age%></p>
    <p>Weight:<%= fighter.weight%></p>
    <p>Height:<%= fighter.height%></p>
    <P>Reach:<%= fighter.reach %></P>
    <p>Wins:<%= fighter.wins%></p>
    <P>Losses:<%= fighter.losses%></P>
    <p>City:<%= fighter.city%></p>
    <p>KO:<%= fighter.ko%></p>
    <p>Submission:<%= fighter.sub%></p>
    <img src="<%= fighter.img%>">
 
    <form action="/fighter/<%= fighter._id%>?_method=delete" method="post">
    <button type="submit"> delete</button>
    </form>
 
    <ul>
      <li><a href="/fighter">back to List </a></li>
      <li><a href="/fighter/<%= fighter._id%>/edit">Update Fighter</a></li>
    </ul>
    
  </ul>
</body>
</html>